/**
 * Re-export from shared device-form validation
 * This file is kept for backward compatibility but now uses the shared validation schema
 */
export { deviceFormSchema, interfaceSchema } from '@/components/shared/device-form/validation'
export type { DeviceFormValues, InterfaceFormValues } from '@/components/shared/device-form/validation'
